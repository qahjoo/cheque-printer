-- =============================================================================
-- 005_images.sql — إضافة حقل الصورة للشيكات الواردة
-- =============================================================================

ALTER TABLE incoming_checks ADD COLUMN image_path TEXT;
